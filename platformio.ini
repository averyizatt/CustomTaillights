; PlatformIO Project Configuration
; ESP32-S3 Custom Foxbody Taillight Controller

[platformio]
default_envs = esp32-s3-pcb

[env:esp32-s3]
; Pin the tested Arduino 3.3.8 / IDF 5.5.4 toolchain.
platform  = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.38-1/platform-espressif32.zip
board     = esp32-s3-devkitc-1
framework = arduino
extra_scripts = pre:scripts/wifi_include.py

; Serial monitor
monitor_speed = 115200

; Libraries
lib_deps =
    fastled/FastLED @ 3.10.3
    autowp/autowp-mcp2515 @ ^1.3.1
    bblanchon/ArduinoJson @ ^7.0.0

; Build flags
build_flags =
    -DCORE_DEBUG_LEVEL=0
    ; Arduino 3.x's GPIO/RGB support links the new RMT driver. Mixing it with
    ; legacy RMT aborts before setup(), so FastLED must use the same driver.
    -DFASTLED_RMT5=1

; Custom PCB build.  Uses the same firmware and hardware features with the
; PCB connector/net pinout selected in config.h.
[env:esp32-s3-pcb]
extends = env:esp32-s3
build_flags =
    ${env:esp32-s3.build_flags}
    -DCUSTOM_TAILLIGHTS_PCB=1

[env:native]
platform = native
test_build_src = no
